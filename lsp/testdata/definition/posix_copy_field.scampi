module main

import "std"
import "std/posix"

std.deploy(name = "x", targets = []) {
  posix.copy {
    src = posix.source_local { path = "/etc/passwd" }
    dest = "/tmp/p"
    pe$0rm = "0644"
  }
}
