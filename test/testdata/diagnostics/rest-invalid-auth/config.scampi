module main

import "std"
import "std/posix"
import "std/rest"

let api = rest.target { name = "api", base_url = "http://localhost/api", auth = "not-valid" }

std.deploy(name = "test", targets = [api]) {
  posix.run { apply = "echo ok", check = "true" }
}
