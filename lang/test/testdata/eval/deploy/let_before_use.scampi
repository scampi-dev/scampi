import "std"
import "std/target"

let vps = target.ssh { name = "vps", host = "10.0.0.1", user = "root" }

std.deploy {
  name = "web"
  targets = [vps]

  let packages = ["nginx", "certbot"]
  std.pkg { packages = packages, source = std.system {} }
}
