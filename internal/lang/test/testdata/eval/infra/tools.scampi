module main

// Translated from .infra/tools.scampi (inlined, no multi-file imports)

import "std"
import "std/posix"
import "std/ssh"

let vps = ssh.target {
  name = "vps"
  host = "203.0.113.10"
  user = "hal9000"
}

std.deploy(name = "tools", targets = [vps]) {
  posix.pkg {
    packages = ["neovim", "htop", "btop"]
    source   = posix.pkg_system {}
  }
}
